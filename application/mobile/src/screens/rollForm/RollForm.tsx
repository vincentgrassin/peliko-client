import React from "react";
import { View, Text } from "../../components";

interface RollFormProps {}

const RollForm: React.FC<RollFormProps> = ({ ...props }) => (
  <View>
    <Text>RollForm</Text>
  </View>
);

export default RollForm;
