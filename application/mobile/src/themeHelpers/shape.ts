const spacing = (n: number): number => 8 * n;

const radius = {
  30: 30
};

const width = {
  buttonBorder: 2
};

const globalShapes = {
  spacing,
  radius,
  width
};

export default globalShapes;
